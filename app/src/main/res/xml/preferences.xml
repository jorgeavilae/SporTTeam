<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout="@layout/fragment_list">
    <CheckBoxPreference
        android:key="pref_sync"
        android:title="pref_sync"
        android:summary="pref_sync_summ"
        android:defaultValue="true" />
    <ListPreference
        android:dependency="pref_sync"
        android:key="pref_syncConnectionType"
        android:title="pref_syncConnectionType"
        android:dialogTitle="pref_syncConnectionType"
        android:entries="@array/sport_id_entries"
        android:entryValues="@array/sport_id_values"
        android:defaultValue="@string/running" />
    <EditTextPreference
        />
</PreferenceScreen>