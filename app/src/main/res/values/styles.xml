<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:listDivider">@color/colorPrimaryLight</item>
    </style>

    <style name="AppTheme.NoActionBar.Login" parent="AppTheme.NoActionBar">
        <item name="colorAccent">@color/colorSportteam_logo</item>
    </style>

    <style name="AppTheme.NoActionBar.UCrop" parent="AppTheme.NoActionBar">
        <item name="android:fitsSystemWindows">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="Theme.AppCompat.Light" />

    <!-- Text style for subtitle in list items -->
    <style name="ListItemSubtitle" parent="TextAppearance.AppCompat.Caption" >
        <item name="android:textSize">@dimen/text_size_body1</item>
    </style>

    <style name="CustomRatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/colorLight</item>
        <item name="colorControlActivated">@color/colorSportteam_logo</item>
    </style>

</resources>
